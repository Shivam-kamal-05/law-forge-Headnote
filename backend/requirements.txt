# Law Lens — Headnote Forge
# Python 3.11+
# Install: pip install -r requirements.txt
#
# SYSTEM BINARIES required for the OCR fallback (scanned / older judgments).
# Without BOTH of these, image-only PDFs fail with an OCR error:
#   Ubuntu/Debian: sudo apt-get install -y tesseract-ocr poppler-utils
#   macOS:         brew install tesseract poppler
# (poppler powers pdf2image; tesseract powers pytesseract.)

fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# Pydantic / settings
pydantic==2.10.3
pydantic-settings==2.7.0

# Anthropic SDK
anthropic==0.40.0

# PDF parsing
PyMuPDF==1.25.1
pdfplumber==0.11.4
pdf2image==1.17.0
pytesseract==0.3.13

# Resilience / rate limiting
tenacity==9.0.0

# Logging / serialisation
structlog==24.4.0
orjson==3.10.12

# Async HTTP (Anthropic SDK dep — pin for compatibility)
httpx==0.27.2
aiofiles==24.1.0
